{
  "name": "server",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "db_generate": "bunx drizzle-kit generate",
    "db_migrate": "bunx drizzle-kit migrate",
    "seed": "bun run src/infrastructure/db/seed/seed.ts",
    "seed:reset": "bun run src/infrastructure/db/seed/seed.ts --reset"
  },
  "dependencies": {
    "@elysiajs/jwt": "^1.4.0",
    "bcrypt": "^6.0.0",
    "drizzle-orm": "^0.45.1",
    "elysia": "latest",
    "jsonwebtoken": "^9.0.3",
    "postgres": "^3.4.8"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "bun-types": "latest",
    "drizzle-kit": "^0.31.8"
  },
  "module": "src/index.js"
}
